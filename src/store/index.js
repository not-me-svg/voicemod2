import { createStore } from 'vuex'
import VoicesModule from '@/store/modules/voices.module'

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    VoicesModule
  }
})
